{
  "buildCommand": "cd registra-marmita && npm install && npx ng build --configuration=production",
  "outputDirectory": "registra-marmita/dist/registra-marmita/browser",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
